第七阶段：真实 sandbox 工具桥验收
时间：2026-08-18（Asia/Shanghai）

主机侧实际结果：
  REMOTE_TOOL_TEST=PASS png_bytes=20911 mouse_move=PASS bash=PASS

API 容器侧实际结果：
  API_CONTAINER_TOOL_TEST=PASS png_bytes=21003

增强动作结果：
  ZOOM_TOOL_TEST=PASS png_bytes=4719

验证内容：
  1. 通过公开 API 创建一次性 session 和独立 Docker sandbox；
  2. 使用数据库返回的 runtime_id 定向连接该容器；
  3. 从 DISPLAY=:1 捕获 scrot PNG 并检查 PNG 文件头；
  4. 移动鼠标到 [100,100] 并获得动作后截图；
  5. 在同一 sandbox 中运行 printf sandbox-ok；
  6. 用 scrot 区域截图验证 computer_20251124 的 zoom 动作；
  7. 从最终 API 镜像的 app 用户与 Docker socket 再次完成截图；
  8. 删除所有临时 session 及对应 sandbox。

说明：没有调用 Anthropic API，没有产生模型费用，没有保存截图文件或 VNC JWT。
